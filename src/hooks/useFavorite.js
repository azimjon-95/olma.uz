export const addHeart = (item, dispatch) => {
  return dispatch({ type: "ADD_TO_HEART", payload: item });
};

export const removeFromHeart = (item, dispatch) => {
  return dispatch({ type: "ADD_TO_HEART", payload: item });
};
